# Coder 自动补全统一使用 Aily Tab。
# 模型、地址和凭证只由宿主认证桥接及服务端 code_suggestions 配置管理。
# 前端不再读取 VITE_AI_INLINE_* 或 aiInlineProvider，也不再提供本地 FIM / v3 回退。
# 编辑器设置：aily.completion.enabled、crossFile、autoImports、languages、excludedExtensions。
